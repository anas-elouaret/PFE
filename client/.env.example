# API base URL. In dev with Vite proxy, this should be "/api".
# In production with a separate API domain, set to the full URL.
VITE_API_URL=/api

# Vite dev server proxy target — the backend server URL.
VITE_API_PROXY_TARGET=http://localhost:5000
