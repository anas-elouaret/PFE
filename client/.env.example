# API base URL — the backend server origin (without /api).
# The /api prefix is appended automatically by the API client.
# Dev: http://localhost:5000
# Production: https://api.yourdomain.com
VITE_API_URL=http://localhost:5000
